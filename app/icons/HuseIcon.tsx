// @format
import React, { ReactElement } from 'react';
import { Icon } from '@ui-kitten/components';

const HouseIcon = (): ReactElement => (
    <Icon name="home" width={24} height={24} />
);

export default HouseIcon;
