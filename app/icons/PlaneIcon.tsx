// @format
import React, { ReactElement } from 'react';
import { Icon } from '@ui-kitten/components';

const PlaneIcon = (): ReactElement => (
    <Icon name="paper-plane" width={24} height={24} />
);

export default PlaneIcon;
